CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
FIND_PACKAGE(OpenCV REQUIRED)

PROJECT(PROJET)

ADD_EXECUTABLE(PROJET main.cpp data.h maxflow/graph.cpp collage.cpp collage.h)
TARGET_LINK_LIBRARIES(PROJET ${OpenCV_LIBS})
